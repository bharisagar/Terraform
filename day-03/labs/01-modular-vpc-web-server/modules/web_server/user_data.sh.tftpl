#!/bin/bash
set -euxo pipefail

dnf update -y
dnf install -y httpd

cat > /var/www/html/index.html <<HTML
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bharisagar Terraform Day 3</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        min-height: 100vh;
        display: grid;
        place-items: center;
        background: #f8fafc;
        color: #111827;
      }
      main {
        max-width: 780px;
        padding: 32px;
      }
      code {
        background: #e5e7eb;
        padding: 2px 6px;
        border-radius: 4px;
      }
    </style>
  </head>
  <body>
    <main>
      <h1>Bharisagar Terraform AWS Day 3</h1>
      <p>This web server was deployed through a reusable Terraform module.</p>
      <p>Project: <code>${project_name}</code></p>
      <p>Environment: <code>${environment}</code></p>
      <p>Region: <code>${region}</code></p>
    </main>
  </body>
</html>
HTML

systemctl enable httpd
systemctl start httpd
